var animation = bodymovin.loadAnimation({
    container: document.getElementById('anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: "mariasi.json"
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('anim_picture'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: "picture.json"
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('arrow'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: "arrow.json"
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('arrow2'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: "arrow-down.json"
})

